import telebot

# ⚠️ Замініть 'YOUR_BOT_TOKEN' на токен вашого бота, отриманий від BotFather
BOT_TOKEN = '8117385329:AAFwTXqVa8Y6VTfoZs64wgwxIiG6xhowfF8'

# Ініціалізація об'єкта бота
bot = telebot.TeleBot(BOT_TOKEN)

# --- Обробник команди /start ---
# Ця функція спрацює, коли користувач надішле команду /start
@bot.message_handler(commands=['start'])
def send_welcome(message):
    # Отримуємо ім'я користувача. 
    # 'first_name' - це поле, яке містить ім'я користувача.
    user_name = message.from_user.first_name
    
    # Формуємо привітальне повідомлення
    welcome_text = f"Привіт, {user_name}! 👋"
    
    # Відправляємо повідомлення назад у чат
    bot.send_message(message.chat.id, welcome_text)

# --- Запуск бота ---
# Бот безкінечно опитує Telegram-сервери на наявність нових повідомлень
print("Бот запущено...")
try:
    bot.polling(none_stop=True)
except Exception as e:
    print(f"Помилка при запуску бота: {e}")